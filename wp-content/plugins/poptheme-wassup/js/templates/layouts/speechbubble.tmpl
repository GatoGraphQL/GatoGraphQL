{{#with itemObject}}
	<div class="{{../classes.bubble-wrapper}}" style="{{../styles.bubble-wrapper}}">
		<blockquote class="{{../classes.bubble}}" style="{{../styles.bubble}}">
			{{#withModule ../. "layout" itemObjectId=id}}
				{{enterModule ../../.}}
			{{/withModule}}
		</blockquote>
	</div>
{{/with}}