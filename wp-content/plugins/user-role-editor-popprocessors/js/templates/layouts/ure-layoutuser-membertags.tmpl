{{#with itemObject}}
	<span class="{{../class}}" style="{{../style}}">
		{{#if ../titles.description}}{{{../titles.description}}} {{/if}}{{labelize membertags-strings ../classes.label}}
	</span>
{{/with}}