{{#with itemObject}}
	<div class="{{../class}}" {{#generateId context=../.}}{{../id}}{{/generateId}}>
		<h1 class="pop-visible-embed author-title">{{{display-name}}}</h1>
		{{#if short-description-formatted}}<p><strong>{{{short-description-formatted}}}</strong></p>{{/if}}
		{{{showmore description-formatted len=../description-maxlength}}}
	</div>
{{/with}}