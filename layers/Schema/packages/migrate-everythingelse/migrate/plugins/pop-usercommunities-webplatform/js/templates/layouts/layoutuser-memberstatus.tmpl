{{#with dbObject}}
	<span class="{{../class}}" style="{{../style}}">
		{{#if ../titles.description}}{{{../titles.description}}} {{/if}}{{labelize memberstatus-byname ../classes.label}}
	</span>
{{/with}}