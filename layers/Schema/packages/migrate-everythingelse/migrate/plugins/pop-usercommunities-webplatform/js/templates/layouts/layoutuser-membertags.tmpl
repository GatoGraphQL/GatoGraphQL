{{#with dbObject}}
	<span class="{{../class}}" style="{{../style}}">
		{{#if ../titles.description}}{{{../titles.description}}} {{/if}}{{labelize membertags-byname ../classes.label}}
	</span>
{{/with}}