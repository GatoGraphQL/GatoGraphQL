"use strict";

// Needed as replaced file wp-content/plugins/wordpress-social-login/assets/js/widget.js
window.wsl_wordpress_social_login = function(config) {

	pop.SocialLogin.fetchLoggedInUserData(config);
}