<span class="media clearfix">
	{{#if avatar-40}}
		<span class="media-left">
			<img width="{{avatar-40.size}}" height="{{avatar-40.size}}" src="{{avatar-40.src}}">
		</span>
	{{/if}}
	<span class="media-body">
		<h4 class="media-heading">{{{display-name}}}<br/><small>{{nicename}}</small></h4>
	</span>
</span>