.phpunit.result.cache
composer*.local.json
/vendor/
/bin/
# Root folder build/ is ignored, but not blocks/**/build/
/build/
/wordpress/
.lando.local.yml
# The cache folder will contained cached configuration files,
# generated by the plugin, so everything there must be ignored
/cache/
