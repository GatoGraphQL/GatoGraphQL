(window.webpackJsonpGraphQLAPISchemaConfigurationAdditionalDocumentation=window.webpackJsonpGraphQLAPISchemaConfigurationAdditionalDocumentation||[]).push([[26],{72:function(e,t){e.exports='<h1 id="inspect-http-request-fields">Inspect HTTP Request Fields</h1> <p>Addition of fields to retrieve the current HTTP request data.</p> <h2 id="description">Description</h2> <p>The following fields to retrieve the current HTTP request data are added to the GraphQL schema:</p> <h3 id="_httprequestbody"><code>_httpRequestBody</code></h3> <p>Body of the HTTP request.</p> <h3 id="_httprequestclienthost"><code>_httpRequestClientHost</code></h3> <p>Client host.</p> <h3 id="_httprequestclientip"><code>_httpRequestClientIP</code></h3> <p>Retrieves the client IP address. If the server is not properly configured (see below), the response is <code>null</code>.</p> <h4 id="configuration">Configuration</h4> <p>The client&#39;s IP address is retrieved from under the <code>$_SERVER</code> global variable, normally from under property <code>&#39;REMOTE_ADDR&#39;</code>. However, different platforms may require to use a different property name to retrieve this information.</p> <p>For instance:</p> <ul> <li>Cloudflare might use <code>&#39;HTTP_CF_CONNECTING_IP&#39;</code></li> <li>AWS might use <code>&#39;HTTP_X_FORWARDED_FOR&#39;</code></li> </ul> <p>The property name to use can be configured in the &quot;Plugin Settings &gt; Server IP Configuration&quot; tab on the Settings page:</p> <p><img src="https://raw.githubusercontent.com/leoloso/PoP/master/layers/GraphQLAPIForWP/plugins/graphql-api-for-wp/docs/implicit-features//../../images/settings-general-client-ip-address-server-property-name.png" alt="Configuring the $_SERVER property name to retrieve the client IP" title="Configuring the $_SERVER property name to retrieve the client IP"></p> <h3 id="_httprequestcookie"><code>_httpRequestCookie</code></h3> <p>Request cookie value.</p> <h3 id="_httprequestcookies"><code>_httpRequestCookies</code></h3> <p>Request cookies.</p> <h3 id="_httprequestdomain"><code>_httpRequestDomain</code></h3> <p>Domain of the requested URL.</p> <h3 id="_httprequestfullurl"><code>_httpRequestFullURL</code></h3> <p>Requested URL (including the query params).</p> <h3 id="_httprequesthascookie"><code>_httpRequestHasCookie</code></h3> <p>Does the request contain a certain cookie?.</p> <h3 id="_httprequesthasheader"><code>_httpRequestHasHeader</code></h3> <p>Does the request contain a certain header?.</p> <h3 id="_httprequesthasparam"><code>_httpRequestHasParam</code></h3> <p>Does the request contain a certain parameter?.</p> <h3 id="_httprequestheader"><code>_httpRequestHeader</code></h3> <p>Request header value.</p> <h3 id="_httprequestheaders"><code>_httpRequestHeaders</code></h3> <p>Request headers.</p> <h3 id="_httprequesthost"><code>_httpRequestHost</code></h3> <p>Host of the requested URL.</p> <h3 id="_httprequestmethod"><code>_httpRequestMethod</code></h3> <p>Request method.</p> <h3 id="_httprequestparams"><code>_httpRequestParams</code></h3> <p>JSON object with all the params (passed via POST or GET).</p> <p>Param values can be:</p> <ul> <li>Strings: <code>?param=value</code></li> <li>Arrays: <code>?someArray[]=1&amp;someArray[]=2</code></li> <li>Array of arrays: <code>?someMatrix[0][0]=3&amp;someMatrix[0][1]=4&amp;someMatrix[1][0]=5&amp;someMatrix[1][1]=6</code></li> <li>Associative arrays (i.e. objects): <code>?someAssocArray[&quot;admins&quot;]=20&amp;someAssocArray[&quot;authors&quot;]=30</code></li> <li>Associative array of arrays: <code>?someAssocMatrix[&quot;admins&quot;][0]=7&amp;someAssocMatrix[&quot;admins&quot;][1]=8&amp;someAssocMatrix[&quot;authors&quot;][0]=9&amp;someAssocMatrix[&quot;authors&quot;][1]=10</code></li> <li>Other combinations</li> </ul> <p>To get the value of the param, we can use fields <code>_httpRequestStringParam</code> and <code>_httpRequestStringListParam</code> for the first two cases respectively, but there are no fields for the other cases.</p> <p>For those, use this field <code>_httpRequestParams</code> to obtain the JSON object, and then retrieve the corresponding value from within.</p> <h3 id="_httprequestprotocol"><code>_httpRequestProtocol</code></h3> <p>Request protocol.</p> <h3 id="_httprequestquery"><code>_httpRequestQuery</code></h3> <p>Query params string.</p> <h3 id="_httprequestreferer"><code>_httpRequestReferer</code></h3> <p>Request referer.</p> <h3 id="_httprequestrequesttime"><code>_httpRequestRequestTime</code></h3> <p>Timestamp of the start of the request.</p> <h3 id="_httprequestscheme"><code>_httpRequestScheme</code></h3> <p>Scheme of the requested URL.</p> <h3 id="_httprequestserverip"><code>_httpRequestServerIP</code></h3> <p>Server IP address.</p> <h3 id="_httprequeststringlistparam"><code>_httpRequestStringListParam</code></h3> <p>Value of a param (passed via POST or GET) of type <code>?param[]=value1&amp;param[]=value2</code>.</p> <h3 id="_httprequeststringparam"><code>_httpRequestStringParam</code></h3> <p>Value of a param (passed via POST or GET) of type <code>?param=value</code>.</p> <h3 id="_httprequesturl"><code>_httpRequestURL</code></h3> <p>Requested URL (without query params).</p> <h3 id="_httprequesturlpath"><code>_httpRequestURLPath</code></h3> <p>Asolute path (starting with &quot;/&quot;) of the requested URL.</p> <h3 id="_httprequestuseragent"><code>_httpRequestUserAgent</code></h3> <p>User agent.</p> '}}]);