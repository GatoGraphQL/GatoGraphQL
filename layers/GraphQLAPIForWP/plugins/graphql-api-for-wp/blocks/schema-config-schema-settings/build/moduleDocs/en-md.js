(window.webpackJsonpGraphqlApiSchemaConfigSchemaSettings=window.webpackJsonpGraphqlApiSchemaConfigSchemaSettings||[]).push([[1],{44:function(e,o){e.exports='<h1 id="settings">Settings</h1> <p>Retrieve the settings from the site (stored in table <code>wp_options</code>), by querying field <code>option</code>.</p> <p>For security reasons, which options can be queried must be explicitly configured.</p> <p>By default, only the following options can be queried:</p> <ul> <li><code>&quot;siteurl&quot;</code></li> <li><code>&quot;home&quot;</code></li> <li><code>&quot;blogname&quot;</code></li> <li><code>&quot;blogdescription&quot;</code></li> <li><code>&quot;WPLANG&quot;</code></li> <li><code>&quot;posts_per_page&quot;</code></li> <li><code>&quot;comments_per_page&quot;</code></li> <li><code>&quot;date_format&quot;</code></li> <li><code>&quot;time_format&quot;</code></li> <li><code>&quot;blog_charset&quot;</code></li> </ul> <h2 id="description">Description</h2> <p>Query field <code>Root.option</code>, passing the required option as field argument <code>name</code>.</p> <p>For instance, this query retrieves the site&#39;s URL:</p> <pre><code class="hljs language-graphql"><span class="hljs"><span class="hljs-punctuation">{</span>\n  optionValue<span class="hljs-punctuation">(</span><span class="hljs-symbol">name</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;home&quot;</span><span class="hljs-punctuation">)</span>\n<span class="hljs-punctuation">}</span></span></code></pre> <h2 id="configure-the-allowed-options">Configure the allowed options</h2> <p>In the &quot;Settings&quot; tab from the Settings, we must configure the list of options that can be queried via <code>option</code>.</p> <p>Each entry can either be:</p> <ul> <li>A regex (regular expression), if it&#39;s surrounded by <code>/</code> or <code>#</code>, or</li> <li>The full option name, otherwise</li> </ul> <p>For instance, any of these entries match option name <code>&quot;siteurl&quot;</code>:</p> <ul> <li><code>siteurl</code></li> <li><code>/site.*/</code></li> <li><code>#site([a-zA-Z]*)#</code></li> </ul> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-settings/../../images/schema-configuration-settings-entries.jpg" alt="Defining the entries for the Settings" title="Defining the entries for the Settings"></p> <p>There are 2 behaviors, &quot;Allow access&quot; and &quot;Deny access&quot;:</p> <p>👉🏽 <strong>Allow access:</strong> only the configured entries can be accessed, and no other can<br/> 👉🏽 <strong>Deny access:</strong> the configured entries cannot be accessed, all other entries can</p> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-settings/../../images/schema-configuration-settings-behavior.jpg" alt="Defining the access behavior" title="Defining the access behavior"></p> '}}]);