(window.webpackJsonpGraphQLAPISchemaConfiguration=window.webpackJsonpGraphQLAPISchemaConfiguration||[]).push([[1],{47:function(e,t){e.exports='<h1 id="schema-configuration">Schema Configuration</h1> <p>A schema configuration is used by Custom Endpoints and Persisted Queries to customize their behavior.</p> <h2 id="description">Description</h2> <p>The schema can be configured with those elements provided by the different installed and enabled modules.</p> <p>Modules can define their own configuration to be applied in the schema through their own option blocks, such as:</p> <ul> <li>Setting the schema as public or private</li> <li>Enabling &quot;admin&quot; elements exposing private data</li> <li>Namespacing the schema</li> <li>Using nested mutations</li> </ul> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-configuration/../../images/schema-configuration.png" alt="Creating a new schema configuration" title="Creating a new schema configuration"></p> <h2 id="how-to-use">How to use</h2> <p>Clicking on the Schema Configurations link in the menu, it displays the list of all of them:</p> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-configuration/../../images/schema-configurations-page.png" alt="Schema Configurations in the admin"></p> <p>A schema configuration is a custom post type (CPT). To create a new schema configuration, click on button &quot;Add New Schema Configuration&quot;, which will open the WordPress editor:</p> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-configuration/../../images/new-schema-configuration.png" alt="Creating a new Persisted Query"></p> <p>To configure the different elements in the schema configuration, click on corresponding input, and it becomes editable. Then select the entries that apply to the configuration. When you are done, click on &quot;Publish&quot;:</p> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-configuration/../../images/editing-schema-configuration.gif" alt="Creating a new schema configuration"></p> <p>Once published, the schema configuration becomes available when editing a Custom Endpoint or Persisted Query:</p> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-configuration/../../images/schema-configuration-in-custom-endpoint.png" alt="Selecting the Schema Configuration in the Custom Endpoint"></p> <p>If the Custom Endpoint or Persisted Query has value <code>&quot;Default&quot;</code>, then the schema configuration selected in the Settings will be used:</p> <p><img src="https://raw.githubusercontent.com/GraphQLAPI/graphql-api-for-wp/master/docs/modules/schema-configuration/../../images/settings-default-schema-configuration.png" alt="Persisted query source"></p> <h2 id="editor-inputs">Editor Inputs</h2> <p>These inputs in the body of the editor are shipped with the plugin (more inputs can be added by extensions):</p> <table> <thead> <tr> <th>Input</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><strong>Expose Admin Data in the Schema</strong></td> <td>Expose "admin" elements in the GraphQL schema (such as field <code>Root.roles</code>, input field <code>Root.posts(status:)</code>, and others), which provide access to private data. If <code>"Default"</code> is selected, the value selected in the Settings is used.</td> </tr> <tr> <td><strong>Mutation Scheme</strong></td><td>Define if to enable mutations, and if the redundant fields from the root must be removed. If <code>"Default"</code> is selected, the value selected in the Settings is used.</td> </tr> <tr> <td><strong>Namespace Types?</strong></td><td>Define if to have all types in the schema automatically namespaced. If <code>"Default"</code> is selected, the value selected in the Settings is used.</td> </tr> </tbody> </table> <p>These are the inputs in the Document settings:</p> <table> <thead> <tr> <th>Input</th> <th>Description</th> </tr> </thead> <tbody><tr> <td><strong>Excerpt</strong></td> <td>Provide a description for the schema configuration.<br/>This input is available when module <code>Excerpt as Description</code> is enabled</td> </tr> </tbody></table> '}}]);