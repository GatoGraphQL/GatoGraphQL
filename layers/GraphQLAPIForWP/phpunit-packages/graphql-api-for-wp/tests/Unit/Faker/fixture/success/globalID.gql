query {
  users(pagination: { limit: 3 }) {
    id
    globalID
  }
  posts(pagination: { limit: 3 }) {
    id
    globalID
  }
  pages(pagination: { limit: 3 }) {
    id
    globalID
  }
  comments(pagination: { limit: 3 }) {
    id
    globalID
  }
}