############################################################
# After having disabled module "User Roles",
# check if the UserRole type is available or not:
# Single endpoint: always disabled
# Admin endpoint: disabled or not via the Settings option
############################################################
{
  __schema {
    userType: type(name: "User") {
      name
    }
  }
}