mutation {
  post(by: {id: 28}) {
    title
    update(input: {
      title: "HTTP caching improves performance 33333"
    }) {
      status
      object {
        title
        update(input: {
          title: "HTTP caching improves performance"
        }) {
          status
          object {
            title
          }
        }
      }
    }
  }
}