{{#with dbObject}}
	<script type="text/javascript">
	(function($){
		$(document).one('component:merged', function() {
			var destination = $('div.pop-append-{{../dbObjectDBKey}}-{{id}}.{{../classes.spinner}}');
			destination.addClass('hidden');
		});
	})(jQuery);
	</script>
{{/with}}