{{#with resolvedObject}}
	<script type="text/javascript">
	(function($){
		$(document).one('component:merged', function() {
			var destination = $('div.pop-append-{{../dbObjectTypeOutputKey}}-{{id}}.{{../classes.spinner}}');
			destination.addClass('hidden');
		});
	})(jQuery);
	</script>
{{/with}}