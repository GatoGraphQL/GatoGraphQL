{{! It will be printed inside data-header attr, so we can't use "double quotes" but only 'single quotes'}}
{{#with resolvedObject}}<p class='{{../class}}' style='{{../style}}'>{{{content-clipped}}}</p>{{/with}}