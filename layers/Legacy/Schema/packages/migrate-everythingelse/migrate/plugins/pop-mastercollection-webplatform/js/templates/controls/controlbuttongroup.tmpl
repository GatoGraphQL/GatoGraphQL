<div {{#generateId}}{{id}}{{/generateId}} class="{{class}}" style="{{style}}" {{#each params}} {{@key}}="{{this}}"{{/each}}>
	{{#each subcomponents}}						
		{{! Comment Leo: the typeOutputKey/resolvedObject below might be null (controlgroup) or have a value (quicklinkgroup)}}
		{{enterModule ../.}}
	{{/each}}
</div>