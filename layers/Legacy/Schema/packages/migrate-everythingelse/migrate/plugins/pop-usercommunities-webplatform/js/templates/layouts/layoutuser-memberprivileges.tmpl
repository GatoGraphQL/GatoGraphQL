{{#with dbObject}}
	<span class="{{../class}}" style="{{../style}}">
		{{#if ../titles.description}}{{{../titles.description}}} {{/if}}{{labelize memberprivileges-byname ../classes.label}}
	</span>
{{/with}}